#ifndef __USER__
#define __USER__

#include <stdio.h>
#include <stdlib.h>
// #include <mysql.h>

// int CreateUser() {
//     FILE *test;
//     test = fopen("user\\list.txt", "rt");
//     int userIdx;
//     if (test == NULL) {
//         userIdx = 1;
//     } else {
//         while (fgets(userIdx, sizeof(userIdx), fp) != NULL) {
//         fprintf(test, "%d", userIdx);
//     }

// }


/*
typedef struct PERSON {
    int idx;
    char name[10];
} person;
*/

/*
1. 유저가 새로 생성되면 고유한 아이디를 추가해줘야함
2. 아이디를 생성하면서 파일을 추가해야함
3. 파일의 리스트를 통해 다음 유저의 아이디를 결정함
*/




#endif